config.py
数据集路径：FS_JUMP3D_DATA_DIR: str = "/home/u2023312269/数据集/FS-Jump3D-main/data/npy"
shq的路径是D:\work\讨论\FS-Jump3D-main\FS-Jump3D-main\data\npy

训练模型
python train.py
指定输出文件名或并行数
python train.py --output-tree "model_fsjump3d.npz" --output-metadata "model_fsjump3d.pkl" --workers 8
 python train.py --output-tree "model_fsjump3d.npz" --output-metadata "model_fsjump3d.pkl" --workers 8 --source-npy r"D:\work\讨论\FS-Jump3D-main\FS-Jump3D-main\data\npy\Skater_A\Axel\Axel_1.npy"                                       

执行查询
python query.py --npy-file "/home/u2023312269/数据集/FS-Jump3D-main/data/npy/Skater_A/Axel/Axel_1.npy" --frame-index 100
python query.py --npy-file r"D:\work\讨论\FS-Jump3D-main\FS-Jump3D-main\data\npy\Skater_A\Axel\Axel_1.npy" --frame-index 100
python query.py --npy-file "*/FS-Jump3D-main/FS-Jump3D-main/data/npy/Skater_A/Axel/Axel_1.npy" --frame-index 100

 python query.py --npy-file <你的npy> --frame-index <idx> --model-tree D:\path\to\model.npz --model-metadata D:\path\to\model.pkl
关键参数说明： 
--npy-file: 待查询的 NPY 文件路径。
--frame-index: 查询该文件中的第几帧（从 0 开始）。
--top-k: 返回最相似的前 K 个结果（默认为 5）。
--model-tree: 指定训练好的模型文件（默认 model.npz 文件）。

## 帧检索系统使用说明（根目录版本 / 推荐）

本系统用于对 **3D 骨架序列的单帧** 建立索引，并基于候选集合 + 精确相似度计算进行检索。
当前根目录脚本支持 **NPY（FS-Jump3D / Human3.6M 17 关节）** 与 **BVH** 两种数据源。

> 注意：仓库里还存在一个 `tree/` 目录（旧版/备份代码）。根目录这份文档对应 **根目录脚本**（`train.py` / `query.py` / `npy_loader.py`）。

---

## 1. 环境准备

### 1.1 Python 与依赖
- **Python**：建议 3.10+（3.8+ 通常也可）
- **最小依赖**（训练/查询必需）：`numpy`、`tqdm`
- **可选依赖**
  - `psutil`：训练时显示内存占用（没有也不影响训练）
  - `plotly`：`utils/visualize_npy.py` 的 3D 可视化
  - `scikit-learn`：`leaf_cluster_validate.py` 的叶节点聚类/跨树验证

安装示例（PowerShell）：

```bash
pip install -r requirements.txt
pip install numpy psutil plotly scikit-learn
```

> 说明：当前 `requirements.txt` 仅包含 `tqdm`，所以建议额外安装上面列出的常用库。

---

## 2. 数据准备（FS-Jump3D NPY）

### 2.1 目录结构约定
系统默认按 FS-Jump3D 的常见目录组织解析动作标签：

```text
<FS_JUMP3D_DATA_DIR>/
  Skater_A/
    Axel/
      Axel_1.npy
      Axel_2.npy
    Lutz/
      Lutz_1.npy
  Skater_B/
    ...
```

其中每个 `.npy` 文件形状应为：
- **(T, 17, 3)**：T 帧、17 个关节、每个关节 3D 坐标

### 2.2 预处理策略（已在加载器中实现）
`npy_loader.py` 在加载单帧时默认会做：
- **中心化**：将 Hip（关节0）平移到原点
- **朝向对齐**：将骨盆方向旋转到统一朝向（面朝 Y 轴正方向）
- **骨骼重定向归一化**：按 `config.STANDARD_BONE_RATIOS` 将骨骼长度归一到统一模板（默认开启）

---

## 3. 配置说明（`config.py`）

常用配置项：
- **数据源类型**：`DATA_SOURCE_TYPE = "npy"` 或 `"bvh"`
- **NPY 数据根目录**：`FS_JUMP3D_DATA_DIR`（默认训练不传 `--data-dir` 时使用）
- **检索返回数量**：`TOP_K`
- **候选帧数量与搜索策略**：`MIN_CANDIDATES`、`BEAM_WIDTH`、`MAX_BACKTRACK_DEPTH`
- **八叉树结构**：`JOINT_PAIRS`、`PAIR_ITERATION_DEPTH`（影响树深度与分裂方式）
- **旋转增强**：`ROTATION_CONFIGS`
  - 训练阶段会对每帧生成多种旋转增强样本，并在 `frame_id` 上追加 `_rot_<axis><angle>` 后缀
  - 默认仅包含 `{"axis": "z", "angle": 0}`（即不增强）

---

## 4. 训练（构建索引）

训练会输出两类文件：
- **树模型**：`.npz`（扁平化八叉树，加载快、体积小）
- **元数据**：`.pkl`（每帧的 `source_file / frame_index / frame_id / keypoints`）

### 4.1 用数据目录训练（推荐）

```bash
python train.py --data-dir "D:\work\讨论\FS-Jump3D-main\FS-Jump3D-main\data\npy" --output-tree "model_fsjump3d.npz" --output-metadata "model_fsjump3d.pkl" --workers 8
```

### 4.2 仅用单个 `.npy` 文件训练（快速验证）
根目录 `train.py` 支持 `--source-npy`（其优先级高于 `--data-dir`）：

```bash
python train.py --source-npy "D:\work\讨论\FS-Jump3D-main\FS-Jump3D-main\data\npy\Skater_A\Axel\Axel_1.npy" --output-tree "model_axel.npz" --output-metadata "model_axel.pkl" --workers 8
```

### 4.3 常用参数
- **`--workers`**：并行加载进程数；不传则默认用 \(CPU-1\)
- **`--source-type`**：强制指定数据源类型（`npy`/`bvh`），不传则使用 `config.DATA_SOURCE_TYPE`
- **`--quiet`**：减少日志输出

---

## 5. 查询（帧检索）

### 5.1 单次查询（NPY）

```bash
python query.py --npy-file "D:\work\讨论\FS-Jump3D-main\FS-Jump3D-main\data\npy\Skater_A\Axel\Axel_1.npy" --frame-index 100 --model-tree "model_fsjump3d.npz" --model-metadata "model_fsjump3d.pkl" --top-k 5
```

### 5.2 单次查询（BVH）

```bash
python query.py --bvh-file "D:\path\to\motion.bvh" --frame-index 10 --model-tree "model.npz" --model-metadata "model.pkl" --top-k 5
```

### 5.3 交互式查询模式
进入交互模式后，按提示输入：`<BVH路径> <帧索引>`（输入 `q` 退出）。

```bash
python query.py --interactive --model-tree "model_fsjump3d.npz" --model-metadata "model_fsjump3d.pkl"
```

### 5.4 路径参数的“目录”用法（小技巧）
根目录 `query.py` 的 `--model-tree/--model-metadata` 支持传 **目录**：若该目录存在，会自动拼接默认文件名（`model.npz` / `model.pkl`）。例如：

```bash
python query.py --npy-file "<你的npy>" --frame-index 0 --model-tree ".\models" --model-metadata ".\models"
```

> 重要：目录必须已存在，否则会被当作普通路径而导致“找不到文件”。

---

## 6. 输出字段说明（查询结果）
查询默认会输出：
- **候选帧数量**：八叉树阶段筛出的候选集合大小
- **八叉树查询用时 / 精确计算用时**
- **Top-K 结果**（每条结果包含）
  - `文件名`：来自 `FrameMetadata.source_file`
  - `帧索引`：原文件中的帧号
  - `帧ID`：NPY 形式通常为 `Skater_JumpType_Filename_frame_XXXX`（旋转增强会追加 `_rot_...`）
  - `距离值`：越小越相似
  - `相似度`：越大越相似

---

## 7. 可视化（NPY 3D 骨架）

脚本：`utils/visualize_npy.py`（使用 Plotly 在浏览器打开）

### 7.1 播放整段动画（默认）

```bash
python utils\visualize_npy.py --path "D:\work\讨论\FS-Jump3D-main\FS-Jump3D-main\data\npy\Skater_A\Lutz\Lutz_1.npy"
```

### 7.2 查看指定帧（静态）

```bash
python utils\visualize_npy.py --path "...\Lutz_1.npy" --frame 100
```

### 7.3 常用参数
- **`--center`**：将 Hip（关节0）置于原点
- **`--no-labels`**：隐藏关节索引标签

---

## 8. 叶节点聚类与跨树验证（可选）

脚本：`leaf_cluster_validate.py`
- 对树 A 的叶节点做 K-Means 聚类
- 使用树 A 的元数据建立 `cluster -> 动作标签` 多数投票映射（避免验证集泄漏）
- 用树 B 的元数据帧在树 A 上下沉到叶节点并评估一致性

示例：

```bash
python leaf_cluster_validate.py --tree-a "model_A.npz" --metadata-a "model_A.pkl" --metadata-b "model_B.pkl" --n-clusters 5 --output "reports\leaf_validate.json"
```

---

## 9. 常见问题（FAQ）

### 9.1 提示“找不到模型文件 model.npz / model.pkl”
- 先确认训练已生成 `.npz/.pkl`
- 查询时显式传入：
  - `--model-tree "<绝对路径>\model.npz"`
  - `--model-metadata "<绝对路径>\model.pkl"`

### 9.2 Windows 路径包含空格/中文导致解析失败
- 建议用双引号包起来：`"D:\路径\含 空格\文件.npy"`
- 也可以用原始字符串形式（仅对 Python 代码有效，对命令行无效）

### 9.3 `frame-index` 越界
如果 `.npy` 总帧数为 T，则合法范围为 `[0, T-1]`。

### 9.4 依赖缺失
- `ModuleNotFoundError: No module named 'plotly'`：安装 `plotly`
- `ModuleNotFoundError: No module named 'sklearn'`：安装 `scikit-learn`

