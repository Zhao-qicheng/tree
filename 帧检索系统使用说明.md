# 帧检索系统使用说明

## 系统概述

这是一个基于八叉树的BVH动作帧检索系统，能够：
1. 从多个BVH文件中加载所有帧进行训练
2. 使用16个关键关节构建空间索引
3. 支持精确匹配和模糊查询
4. 返回Top-K个最相似的帧，并计算相似度得分

## 核心特性

- **16个关键关节**: hip, chest, neck, head, l/rShldr, l/rForeArm, l/rHand, l/rThigh, l/rShin, l/rFoot
- **高精度坐标**: 6位小数精度
- **加权相似度**: 手/脚/前臂/小腿权重更高
- **多分枝八叉树搜索（Beam Search）**: 即便查询样本位于树结构空洞区，也会根据节点“接近度”同时向多个子节点继续下探，避免回退到根节点导致全量扫描
- **受控回溯**: 候选帧不足时仅回溯有限的层级（默认 2 层），防止候选集爆炸
- **Top-K查询**: 默认返回最相似的5个帧
- **模型缓存**: 查询脚本默认复用已加载模型，避免重复加载
- **多核并行**: 训练与查询都可利用多核 CPU 加速
- **进度提示**: 保存/加载模型时输出分步骤进度，便于掌握耗时阶段

## 文件结构

### 核心代码
- `config.py` - 系统配置（关键点、精度、权重等）
- `data_loader.py` - BVH文件加载
- `data_structures.py` - 数据结构定义
- `octree_node.py` - 八叉树节点
- `octree_builder.py` - 八叉树构建和序列化
- `similarity.py` - 相似度计算
- `train.py` - 训练脚本
- `query.py` - 查询脚本

### 生成的模型文件
- `model.tree` - 八叉树索引（二进制pickle）
- `model.pkl` - 帧元数据（pickle）

## 使用流程

### 1. 训练模型

从data目录加载所有BVH文件并构建索引：

```bash
python train.py --data-dir data_train/
```

默认会依据 `config.JOINT_PAIR_GROUPS` 定义的关节对构建多棵八叉树索引。若 `--output-tree` 为 `model.tree`，训练结束后将得到 `model_chest_neck.tree`、`model_lForeArm_lHand.tree` 等多个文件以及 `model_pairs.json`（用于记录关节对与树文件的映射）。需要单棵树时，可将 `config.JOINT_PAIR_GROUPS` 设为空元组。

可选参数：
- `--output-tree <path>`: 指定树文件输出路径（默认: model.tree）
- `--output-metadata <path>`: 指定元数据输出路径（默认: model.pkl）
- `--workers <int>`: 并行加载BVH的进程数（默认=CPU核心数-1，设为1可关闭并行）
- `--quiet`: 静默模式，不打印详细信息

训练输出示例：
```
================================================================================
开始训练帧检索模型
================================================================================

步骤1: 扫描 data/ 目录...
找到 14 个BVH文件:
  - 01_01.bvh
  - 01_02.bvh
  ...

步骤2: 创建八叉树根节点...
根节点创建成功，深度: 0
八叉树使用的关键点数量: 9

步骤3: 加载并插入所有帧...
处理文件: 01_01.bvh (300 帧)
  已处理 100 帧...
  已处理 200 帧...
  已处理 300 帧...

训练完成!
  成功加载帧数: 4200
  总节点数: 5821
  叶节点数: 2134
```

### 2. 查询帧

查询指定BVH文件的某一帧：

```bash
python query.py --bvh-file data_test/01_01.bvh --frame-index 10
```

可选参数：
- `--model-tree <path>`: 树模型文件路径（默认: model.tree）
- `--model-metadata <path>`: 元数据文件路径（默认: model.pkl）
- `--top-k <int>`: 返回前K个结果（默认: 5）
- `--interactive`: 进入交互式查询模式（加载一次模型，多次查询）
- `--quiet`: 静默模式

查询输出示例：
```
================================================================================
帧检索查询系统
================================================================================

步骤1: 加载模型...
  成功加载八叉树模型: model.tree
  成功加载元数据: model.pkl
  训练集帧数: 4200

步骤2: 查询帧关键点位置...
--------------------------------------------------------------------------------
  hip         : X=   0.000, Y=   0.000, Z=   0.000
  chest       : X=   0.123, Y=  20.456, Z=  -0.789
  neck        : X=   0.045, Y=  32.123, Z=  -1.234
  ...
--------------------------------------------------------------------------------

步骤3: 查找Top-5相似帧...

查询结果:
================================================================================

[精确匹配] 找到完全相同的帧！
--------------------------------------------------------------------------------

排名 1: [精确匹配]
  文件名: 01_01.bvh
  帧索引: 10
  帧ID: 01_01_frame_0010
  距离值: 0.000000
  相似度: 1.0000

Top-5最相似的帧:
--------------------------------------------------------------------------------

排名 1: [精确匹配]
  文件名: 01_01.bvh
  帧索引: 10
  帧ID: 01_01_frame_0010
  距离值: 0.000000
  相似度: 1.0000

排名 2:
  文件名: 01_01.bvh
  帧索引: 11
  帧ID: 01_01_frame_0011
  距离值: 2.345678
  相似度: 0.8234

排名 3:
  文件名: 01_01.bvh
  帧索引: 9
  帧ID: 01_01_frame_0009
  距离值: 2.456789
  相似度: 0.8156
...
```

### 3. 交互式多次查询

如果需要在一次运行中重复查询多个帧，可以使用交互式模式。该模式会在启动时加载一次模型，之后所有查询都复用内存中的树和元数据，彻底消除重复加载耗时。

```bash
python query.py --model-tree model.tree --model-metadata model.pkl --interactive
```

在提示符中输入 `路径 帧索引`（例如 `data_test/01_01.bvh 42`），输入 `q` / `quit` / `exit` 可退出交互模式。

# 查询策略与配置

## 多分枝向下搜索

`query.py` 中的 `find_candidate_frames_from_tree` 使用 **Beam Search** 策略：  
- 每一层根据“查询样本与子节点包围盒中心的距离”选出距离最近的 `config.BEAM_WIDTH` 个子节点继续向下遍历  
- 如果某些路径提前到达叶节点，会立即记录其帧ID  
- 候选帧仍不足时，只允许向上回溯 `config.MAX_BACKTRACK_DEPTH` 层，避免回退到根节点而退化成全量遍历  

训练阶段会针对 `config.JOINT_PAIR_GROUPS` 中每个关节对构建一棵独立的八叉树。查询时会在所有树上同时定位候选帧，并按“出现次数”合并候选集合，再进入精确距离计算。这样即便查询样本位于模型未覆盖的区域，也能优先返回最接近的子区域帧，而不是把所有帧都作为候选集。

## 关键配置项

`config.py` 中可调节以下参数以适配不同数据规模：

- `MIN_CANDIDATES`: 候选帧下限  
- `BEAM_WIDTH`: 每层同时保留的节点数量（默认 4）  
- `MAX_BACKTRACK_DEPTH`: 回溯层级上限（默认 2）  
- `JOINT_PAIR_GROUPS`: 参与建模的关节对列表（影响生成的树文件和联合策略）  

## 模型加载 / 保存进度

`octree_builder.save_tree` 与 `load_tree` 会输出如下分步骤信息：

```
[保存模型] 步骤1/3: 清除 parent 引用...
[保存模型] 步骤2/3: 写入 model.tree ...
[保存模型] 步骤3/3: 恢复 parent 引用...
```

在大型模型训练/查询时，可直观地看到当前正在执行的阶段以及节点/引用处理进度，便于估算剩余时间。

### 4. 运行测试

验证系统功能：

```bash
python test_frame_retrieval.py
```

测试包括：
1. 训练模型测试
2. 精确匹配查询测试
3. 模糊查询测试（添加噪声）
4. 跨文件查询测试
5. 性能统计测试

## Python API使用

### 训练模型

```python
from train import train_model

train_model(
    data_dir="data/",
    model_tree_path="model.tree",
    model_metadata_path="model.pkl",
    verbose=True
)
```

### 查询帧

```python
from query import query_from_bvh, _load_model_once

# 方式1：单次查询（脚本内部自动缓存模型）
results = query_from_bvh(
    bvh_file="data/01_01.bvh",
    frame_index=10,
    model_tree_path="model.tree",
    model_metadata_path="model.pkl",
    top_k=5
)

# 处理结果
for i, result in enumerate(results, 1):
    print(f"排名 {i}:")
    print(f"  帧ID: {result.frame_metadata.frame_id}")
    print(f"  距离: {result.distance:.6f}")

# 方式2：手动复用已加载模型（批量查询时更高效）
bundle, _ = _load_model_once("model.tree", "model.pkl")
queries = [
    ("data/01_01.bvh", 10),
    ("data/01_01.bvh", 25),
]
for path, frame_idx in queries:
    results = query_from_bvh(
        bvh_file=path,
        frame_index=frame_idx,
        model_tree_path="model.tree",
        model_metadata_path="model.pkl",
        model_bundle=bundle,
        use_cache=True,
    )
    print(path, frame_idx, "->", results[0].distance)
```

### 使用自定义关键点查询

```python
from query import query_frame
import numpy as np

# 自定义关键点坐标
query_keypoints = {
    'hip': np.array([0.0, 0.0, 0.0]),
    'chest': np.array([0.123, 20.456, -0.789]),
    'neck': np.array([0.045, 32.123, -1.234]),
    # ... 其他关键点
}

results = query_frame(
    query_keypoints=query_keypoints,
    model_tree_path="model.tree",
    model_metadata_path="model.pkl",
    top_k=5
)
```

## 相似度计算说明

### 加权欧氏距离

系统使用加权欧氏距离计算帧之间的相似度：

```
distance = Σ (weight[joint] × ||query[joint] - stored[joint]||₂)
```

其中joint遍历所有9个用于八叉树的关键点（不包括hip原点）。

### 关节权重

在`config.py`中定义：

```python
JOINT_WEIGHTS = {
    "hip": 1.0,
    "chest": 1.0,
    "neck": 0.8,
    "head": 0.6,
    "lShldr": 0.8,
    "rShldr": 0.8,
    "lHand": 1.2,  # 手部更重要
    "rHand": 1.2,
    "lFoot": 1.2,  # 脚部更重要
    "rFoot": 1.2,
}
```

### 相似度得分

距离值通过指数函数转换为0-1之间的相似度得分：

```
similarity = exp(-distance / scale)
```

- similarity = 1.0: 完全相同
- similarity = 0.0: 完全不同

### 精确匹配判断

当加权距离小于阈值时，判定为精确匹配：

```
is_exact_match = (distance < EXACT_MATCH_EPSILON)
```

默认阈值：`EXACT_MATCH_EPSILON = 0.001`

## 性能优化建议

1. **减少训练帧数**: 如果数据集很大，可以跳帧采样
   ```python
   # 每5帧采样一次
   for frame_index in range(0, frame_count, 5):
       ...
   ```

2. **调整八叉树深度**: 修改`config.MAX_DEPTH`（默认12）
   - 深度越大，空间划分越细，但节点数增多
   - 深度越小，查询速度更快，但精度降低

3. **调整关节权重**: 根据应用场景修改`config.JOINT_WEIGHTS`

4. **使用SSD存储**: 将模型文件放在SSD上以提高加载速度

## 故障排除

### 问题1: 找不到某个关节

```
ValueError: 关节 'lHand' 在文件 'xxx.bvh' 中不存在
```

解决方案：检查BVH文件的关节命名，可能需要调整关节映射。

### 问题2: 内存不足

训练大量帧时可能出现内存问题。

解决方案：
- 减少训练帧数（跳帧采样）
- 增加系统内存
- 分批训练（需要修改代码）

### 问题3: 查询速度慢

解决方案：
- 减小`MAX_DEPTH`
- 确保模型文件在SSD上
- 减小`top_k`值

## 扩展功能

可以基于此系统实现的功能：

1. **动作分类**: 添加标签系统，对相似帧进行分类
2. **动作合成**: 基于相似帧进行插值生成新动作
3. **异常检测**: 识别与训练集差异很大的帧
4. **动作检索引擎**: 构建大规模动作数据库的搜索系统
5. **可视化**: 绘制相似度矩阵、t-SNE降维可视化等

## 技术细节

- **坐标系统**: 相对于hip的局部坐标系
- **序列化格式**: Python pickle（protocol=HIGHEST_PROTOCOL）
- **八叉树编码**: 9位组合索引（每位0-7）
- **精度**: 6位小数（约0.000001单位）

## 联系与支持

如有问题或建议，请参考代码中的注释或测试脚本。

