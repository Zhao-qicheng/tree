# 优化八叉树以控制分类数目计划 (Goal Description)

分析目前八叉树在数据量增大时分类数（叶子节点数）激增的原因，并探讨在**不减少关键关节数**的前提下，对树结构的优化方案。

## 现状：为什么分类数会激增？

在您目前的 

config.py 和 

octree_builder.py

 实现中：



1. **深度固定且全量分裂**：树的深度由 `JOINT_PAIRS` 决定（目前是3层：躯干、手腕、脚踝）。每插入一帧数据，树都会顺着这些层级**强制分裂到底**（执行 `MAX_DEPTH` 次划分）。
2. **缺乏容量限制（Threshold）**：即使是一个偶尔产生的噪点，或是动作游走在空间交界边缘引起的微小偏移，只要越过包围盒中线，就会立刻生成一个新的叶子节点并被视为一个新的“分类”。
3. **空间组合爆炸**：每一层受两个关节控制，这就提供了最多 $8 \times 8 = 64$ 个空间格子。3 层最深可产生 $64^3 = 262144$ 个潜在叶子节点。当数据量上升到7万帧后，数据点充分填满了这庞大的空间，必然导致细碎类别（叶子节点）的数量激增至数千个。

## 用户思路分析：为什么减少关节数有局限性？

您提到的“减少参与分类的关节数”确实可以指数级降低细分出的格子总数，但这就好比：为了降低分类数量直接忽略掉了手臂的动作——这会导致所有只有下半身一致但手臂动作截然不同的姿态，硬被归为了同一类。这与动作识别和精准分类的目标是矛盾的。我们需要的是**在保留特征维度的前提下，压缩冗余、低频的空间划分**。

------

## 💡 拟议的树结构优化方案 (Proposed Changes)

为了保留所有关节特征并有效限制叶子节点数量，我为您规划了以下几条优化方向：

### 方案 A：引入后置剪枝与容量阈值合并 (Post-Pruning & Node Merging)

**这是我最推荐且最容易平滑接入现有代码的方案。** 目前树是按帧插入构建临时节点（

_BuilderNode），最后压平（

build_flat_tree

）。 **思路**：在最终压平之前，引入一次**剪枝操作**。我们设置一个节点最小样本阈值（譬如 `MIN_SAMPLES_PER_LEAF = 20`）。从树的底部往上扫描：如果某分支节点的所有子节点包含的总帧数极小，或者很多叶子各占 1-2 帧，我们就把这些碎片的叶子节点**剪掉**，使其父节点重新成为叶子节点。



- **优点**：大幅消灭了微小抖动/游离数据产生的大量微小分类（将它们包容在了更粗一级的父亲节点里）。核心的（密集的）动作类别仍然保持最深的分辨率。

- 修改文件

  ：主要修改

   

  octree_builder.py

   

  补充一个

   

  ```
  prune_tree(root, min_samples)
  ```

   

  函数。

### 方案 B：基于方差驱动自适应层级 (Variance-Driven Depth)

**思路**：目前的树是静态层级先分躯干、再分手腕身、再分踝关节。这不太智能，如果某些动作脚根本不动，我们也在第三层强行切了一刀。可以改为**数据驱动构建**（类似 KD-Tree 的中位数方差切分）：所有数据先集中在根，计算包围盒内哪个关节的坐标方差最大（运动最剧烈），就只在这层针对该关节进行八叉树一分为八，不动的关节不切。直到节点内的数据量少于某个阈值停止分裂。

- **优点**：生成的树极度紧凑。完全消除冗余划分。

- 缺点

  ：需彻底重写

   

  octree_builder.py

   

  的增量插入流，改为全量数据的离线构建计算，改动比较大。

### 方案 C：叶子节点的相似性聚类 (Leaf Clustering / K-Means融合)

**思路**：既然树是用来快速检索空间的，分类多其实不是坏事——它是自然数据的呈现。我们可以在八叉树的顶层逻辑之上，将这 2500 个细分叶子计算中心点坐标（均值），然后对这 2500 个中心点运行一次无监督 K-Means 聚类，将其统一折叠映射为例如 200 个“宏分类 (Macro Classes)”。

- **优点**：不需要改动底层的树数据结构。检索速度依然极快，但对外输出时，相似的格子被归入了同一个业务含义上的组。

------

## 实施建议总结 (Next Steps)

我推荐实施 **方案 A (剪枝与阈值合并)**，或者结合使用 **方案 A 保底去噪 + 方案 C 的更高级宏分类**。

如果您同意，我可以为您实施方案 A（在 `octree_builder` 中加入剪枝逻辑），这不仅控制了树体积和类别数，还能极大减轻模型在极少数样本节点上的不稳定性。

## Verification Plan

### 自动化验证与对照 (Automated Tests)

1. 在

    

   config.py

    

   中将会添加

    

   ```
   MIN_SAMPLES_PER_LEAF
   ```

    

   开关配置参数。

2. 运行原有的短时测试训练集，记录树扁平化前的节点数。同时编写一个小脚本加载新的树结构并输出总叶子节点分布情况（比如多少个叶子包含少于10帧的数据）。

3. 验证功能依然可以编译、保存到 NPY 文件并在

    

   query.py

    

   里被正常使用。

------

请您审查这些思路，如果您觉得剪枝方案合理并偏向这种做法，我可以立即开始为您改写代码。如果有其他倾向（比如方案 C）也请指点！

